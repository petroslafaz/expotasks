// Type definitions for shared types
import { Project as ProjectType } from "@/modules/projects/schema/project-schema";
import { Task as TaskType } from "@/modules/tasks/schema/task-schema";

export { ProjectType, TaskType };
