import { Box, Text } from "@/theme";

export default function Tab() {
  return (
    <Box flex={1} alignItems="center" justifyContent="center">
      <Text variant="body">Tab [Settings]</Text>
    </Box>
  );
}
